# v1.3.2 — PI label cleanup

The per-tile slider was labeled `Samples (this key)` — a remnant from the two-slider design where the parenthetical disambiguated. Since v1.3.1 only ever shows one slider, the qualifier was redundant.

- **PI label** `Samples (this key)` → `Samples` across all 12 action PIs.

No code changes. 183 tests unchanged.
